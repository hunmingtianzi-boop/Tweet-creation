interface:
  display_name: "Organization WeChat Studio"
  short_description: "Build source-zero, Ardot-native WeChat article systems"
  brand_color: "#1648C8"
  default_prompt: "Use $org-wechat-studio to build a source-zero organization pack, calibrate visuals from current materials, create article-specific assets, and assemble an Ardot-native WeChat article."
policy:
  allow_implicit_invocation: true
