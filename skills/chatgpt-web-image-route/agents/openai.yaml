interface:
  display_name: "ChatGPT Web Image Route"
  short_description: "Generate and validate source images through ChatGPT web"
  default_prompt: "Use $chatgpt-web-image-route to create the requested source image in ChatGPT web, download the original, and validate or derive a true RGBA cutout before returning it."
policy:
  allow_implicit_invocation: true
