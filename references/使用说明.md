# 组织型公众号工作流使用说明

这套工作流用于“换一个组织或公众号，仍能继续生成属于它的文章、视觉素材和微信交付文件”。

当前可执行发布版只支持 Codex Desktop。第一次拉取或换机器时，必须先读[克隆、安装与登录前置条件](host-prerequisites.md)，安装同一 release 的三个仓库 Skill，另行安装并把 `codex-with-chatgpt` 绑定到当前精确 workspace，准备内置 Browser 的 ChatGPT 登录、Ardot Remote 登录与目标 file/root 权限；`delivery/full` 再准备目标微信公众号。其他 LLM/harness 只能阅读迁移契约，不能直接运行或声称兼容。

## 使用归属

使用本仓库工作流制作的公开推文，在所有正文和自定义 footer 之后必须保留一次可见文本：`感谢拓浙 AI 生态提供本篇内容生产工作流支持。`。它是仓库级使用归属，不是组织品牌文案；Ardot 中保留末位原生可编辑文本，编译、handoff v5 和草稿回读均会校验。

## 零、启动环境自检

任何组织材料读取、Ardot 创建或公众号操作之前，先从源码 clone 运行 `release_skills.py clone-check --phase <phase>`，再按[运行环境启动自检](runtime-preflight.md)执行平台与 live-session 审计。当前 Codex Desktop 由已验证安装 release + adapter allowlist + 本会话实际 model-visible registry id 生成非签名 census intent，再由 census + 紧凑 target 文件生成 profile。不要手写 Skill `loaded/available`、provider/session、host export 或本地 processor 状态。profile 必须绑定当前 worktree、adapter、installed release、registry census、实际 callable 和无凭据 Ardot/微信链接。把当前模型注册表中每个实际可见 ID 作为 `clone-check --visible-tool-id ID` 重复参数传入。返回 `current_task_reload_required: true` 时先重载/新开 Codex 任务，不读材料、不打开 Ardot 也不继续写公众号。

先从已加载的顶层 `org-wechat-studio/SKILL.md` 解析绝对
`ORG_WECHAT_RUNTIME_ROOT`，再将用户项目中 Git-ignored、create-once 的绝对
目录定义为 `ORG_WECHAT_SESSION_ROOT`。保持用户项目为 cwd，不 `cd` 到
安装包，不在 Skill 下写 `output/`。macOS 临时路径必须使用解析后的
`/private/tmp/...`，不使用 `/tmp/...`。

除下面 create-once release manifest 安装自检外，本文所有公开工作流 CLI
都必须使用绝对 `ORG_WECHAT_RUNTIME_ROOT` 下的 `secure_runner.py` 和绝对
target；组织包的 `list/search` 也不例外。

```bash
ORG_WECHAT_RUNTIME_ROOT=/ABSOLUTE/SKILLS_ROOT/org-wechat-studio
ORG_WECHAT_SESSION_ROOT=/ABSOLUTE/USER/PROJECT/output/runtime/SESSION_UNIQUE

python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/release_skills.py" verify-installed \
  /ABSOLUTE/SKILLS_ROOT/.org-wechat-release-manifests/RELEASE_SHA.json \
  --skills-root /ABSOLUTE/SKILLS_ROOT

python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" --platform-audit

python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/runtime_preflight.py" \
  init-current-session-census \
  --phase migration --session-id CURRENT_HOST_SESSION_ID \
  --visible-tool-id image_gen__imagegen \
  --visible-tool-id view_image \
  --visible-tool-id codex-with-chatgpt \
  --visible-tool-id browser:control-in-app-browser \
  --visible-tool-id mcp__node_repl__js \
  --skills-root /ABSOLUTE/INSTALLED/SKILLS/ROOT \
  --release-manifest /ABSOLUTE/INSTALLED/SKILLS/ROOT/.org-wechat-release-manifests/RELEASE_SHA.json \
  --workspace-root "$ORG_WECHAT_RUNTIME_ROOT" \
  --output "$ORG_WECHAT_SESSION_ROOT/registry-census-UNIQUE.json"

python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/runtime_preflight.py" init-profile \
  "$ORG_WECHAT_SESSION_ROOT/registry-census-UNIQUE.json" \
  "$ORG_WECHAT_SESSION_ROOT/target.json" \
  --workspace-root "$ORG_WECHAT_RUNTIME_ROOT" \
  --phase migration \
  --output "$ORG_WECHAT_SESSION_ROOT/migration-profile-UNIQUE.json"
```

启动默认要求：

- 每次换 clone、机器、Codex 会话、adapter/provider route 或 trusted bundle 时，先跑 `--phase migration --binding-only`。该阶段绑定 runtime/provider session，不生成 `neutral-rgba-route-probe-v1`，也不因 RGBA 检测阻断读材料；
- `authoring/full` 同时绑定 `image.generate.opaque`、RGBA 生产路由与 `image.inspect`。Codex Desktop 默认的 RGBA 路由是 `chatgpt-web-image-route` + `codex-with-chatgpt` + 内置 Browser，再经 `scripts/prepare_micro_cutout.py`；本地 `image_gen__imagegen` 只可承担不透明源图，不得冒充 Alpha 生产器；
- `ardot.read` + `ardot.write` + `ardot.export`，或通过实际登录探测的 UI 路线；
- `wechat.draft`，或能读取当前目标账号的 Browser/Computer Use 路线；草稿 API 不推导 `wechat.current-session-authority`，当 `terminal_state=publish` 且 `mode=api` 时还必须有独立发布权限或 portable receipt，否则只能保持草稿终态或选已声明 UI live route；
- `host.migration.finalize`、`filesystem.access.lease`、`host.receipt.attest` 与 provider policy hook 是可选的保证/政策升级，缺少它们不是登录错误，也不单独阻断 `authoring/full`。正式微组件绑定当前 runtime session/request/create-once ingestion/raw 链；透明素材只检查自身质量，不运行启动 RGBA 探针。结果标记 operator/harness-trusted、`host_attested=false`、`portable=false`；
- `secret.resolve` 只在当前 `artifact_inventory` 存在 eligible watermark carriers 时要求；无载体的完整清单不因缺水印 secret 阻断；
- Python、Pillow、cryptography（Ed25519 验签）、全部本地引用与项目 Skill SHA 一致。安装后 runtime 可为只读；迁移写入与可写探针只落在已验证的 Git-external/Git-ignored `--session-root`。

Ardot 启动报告分别显示 `configured`、`model_visible`、
`live_authenticated/target_access_verified` 和 `last_mutation_outcome`；任何一层不得
由前一层推导。微信 API 也先用 `wechat_publisher.py preflight-account` 只读验证
`draft/count` + `material/get_materialcount`，再单独验证上传、草稿写入、浏览器回读和发布。

不要把 token、Cookie、AppSecret、密钥、ChatGPT 对话 URL/配对码或当前微信带 token 的编辑 URL写入 profile。`migration/authoring/full` 在读材料前执行一般 host setup actions，但 `run-migration-rgba-route-probe` 为非阻断兼容诊断，不执行也能继续。C2C update/sandbox、精确 worktree session、tunnel choice、setup、project/connector/workspace identity、workspace-info 和 doctor 必须分别闭合。Browser 生成真实文章 original 后，再将宿主返回的绝对路径交给 `scripts/ingest_browser_download.py` 并对该素材做所需质量检查。

新组织没有 Ardot file/root 时不要伪造链接：迁移自测通过后，再用 `--phase bootstrap --binding-only` 检查 `ardot.create`，宿主轨迹确认 create-design/page callable 后只创建空白设计，随即用新 file/root 重跑目标阶段（默认 `full`，明确只做 Ardot 时为 `authoring`）。`bootstrap` 不要求生图、ChatGPT、验图、水印 secret、微信账号或登录。

## 一、已有组织包时

查看可用组织：

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/orgs.py" list
```

验证组织包：

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/orgs.py" validate organizations/<organization-id>
```

查看某类文章适合的视觉路线与组件：

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/orgs.py" \
  recommend organizations/<organization-id> recruitment
```

搜索可复用素材：

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/orgs.py" \
  search organizations/<organization-id> "招新 校园"
```

## 二、迁移到新公众号

先使用上一节与当前宿主匹配的 census 命令和 `init-profile --phase migration` 生成最小 profile。它不含 Ardot/微信链接，绑定当前 provider route 与真实 `generation_route_id`。然后运行：

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/runtime_preflight.py" \
  "$ORG_WECHAT_SESSION_ROOT/migration-profile-UNIQUE.json" \
  --workspace-root "$ORG_WECHAT_RUNTIME_ROOT" \
  --session-root "$ORG_WECHAT_SESSION_ROOT" \
  --phase migration --binding-only \
  --output "$ORG_WECHAT_SESSION_ROOT/migration-binding-UNIQUE.json"
```

立即执行一般 `host_setup_actions`，但跳过非阻断的 `run-migration-rgba-route-probe`。组装 `org-wechat-runtime-session-evidence-v1`，绑定报告给出的 nonce/digest、release、registry、adapter、route 与当前 provider session ID，再运行 `finalize-current-session-migration` 得到 `operational_ready: true` 的同会话续跑文件。这个过程不生成图片，不做 Alpha 或三底检查，也不阻断读取组织材料。

进入 `bootstrap/authoring/delivery/full` 时，当前会话路径要用新阶段和当时实际可见 registry id 重跑 `init-current-session-census`，再从新 census 生成 profile；阶段绑定的 migration census 不可直接复用。`authoring/full` 继续引用已建立的同会话 runtime binding，不生成 smoke image。

首先为该公众号建立独立组织包：

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/orgs.py" init new-account-id \
  --name "新组织或公众号名称" \
  --root organizations
```

将生成：

```text
organizations/new-account-id/
├── organization.json
├── sources.json
├── components.json
├── assets.json
├── ardot.json
└── assets/
    ├── official/
    ├── photos/
    ├── generated/
    └── derived/
```

然后完成组织调研：

1. 收集本轮明确允许的官网说明、原始文案、Logo、活动照片与品牌规范。默认不要打开旧推文布局、旧截图/PDF、旧 Ardot 或其他组织 pack；获准的历史文字只能用于 voice/fact，不能用于视觉校准。只有用户明确要求学习某个风格时，才进入 [style-options.md](style-options.md) 的 `explicit-style-grammar` 分支。
2. 在 `sources.json` 中记录来源和可核验事实。
3. 在 `organization.json` 中确认受众、内容支柱、语气、视觉色彩、视觉母题和文章类型。
4. 默认在 `organization.provenance` 填写 `visual_reference_policy: source-zero`、本轮 `visual_input_source_ids`、显式 `visual_input_allowed_roots`（默认 `inputs/current`）、四类旧视觉排除项和 `isolation_reviewed_at`。每个 source 同时写当前内容 SHA-256；父级 symlink、allowlist 外路径、`output/examples/other-org`或“旧稿/往期/成稿”目录一律阻断。若用户明确选了 style preset，则改用 `explicit-style-grammar` 的来源、scope、review time 与六项 non-copy 契约；后续组织把 preset JSON 登记为来源，不重新打开最初参考。
5. 为每个文章类型提出 2–3 条视觉路线，先在全新 Ardot 页面做 Hero、章节、照片编排、微型装饰语言和信息密度小样，不直接做全文。微型装饰语言只用 Ardot 原生可编辑矢量/填色，不生图、不登记资产、不作为 RGBA 探针；首枚真实且与本文相关的已选小组件才实测图像路线。已审核 preset 只能作为其中一个 route 级选项；未带 `style_grammar` 的 sibling routes 仍是 source-zero。Hero/章节同时对比可编辑表现型标题字，至少批准两个 construction recipe；每个 recipe 至少两种非字体手法与两个可编辑图层，只换字体不算艺术字。若启动时选择生成 AI 底图，先做一个母版与 1–3 个同系列变体，整个 family 只能选一种 `light`/`dark` surface mode，并登记 family/variant、归一化 copy-safe zone、正文颜色、最低 4.5 对比度和复制区最大 0.10 亮度标准差，不能逐章随机生成；若选择不生成，则省略 family，以 Ardot 原生表面完成同样的连续阅读与对比度校准。组织包默认启用 `generated_image_watermark.mode: required`；密钥来自仓库外的 `PROVENANCE_WATERMARK_KEY`，必须是以 `hex:` 或 `base64:` 表示的至少 32 个随机字节，不能写进组织包。

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/build_visual_directions.py" \
  organizations/new-account-id recruitment \
  --background-mode generated-family \
  --output output/new-account-id/visual-directions.json
```

仓库当前内置的可选 preset 可在 `style-presets/` 查看。`prismatic-paper-editorial` / 绚烂纸本是暖纸、水彩洗染、干刷与颗粒的连续阅读面语法，不是默认配色模板。选择时只把 preset 的 `grammar` 写入被选 route，组织色、字体、照片与组件仍由本组织材料重新校准。`orgs.py validate` 会把 route SHA 与仓库 canonical preset 对照；修改 token 后重新签一个自有 SHA 也不能通过。

6. 用户批准小样后，把 route、typography recipes 与 Ardot 文件/页面/节点回写到 `organization.visual.calibration`；只有选择生成底图时才写 background family。生成式路线先登记最终 master/companion PNG，再运行下面的验证；它会直接分析像素，阻断明暗跳变、大块黑白、复制区杂乱和文字对比不足。只有相应路线通过后才创建文章 root。组织事实与视觉同时确认后，才将状态改为 `confirmed`。`provisional` 组织会被阻断在全文之前。

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/orgs.py" validate organizations/new-account-id
```

如果同一组织有多个公众号：

- 语气、受众、视觉与栏目完全一致时，可共用组织包，只更换发布连接。
- 如果是子品牌、校园分会、招新号或不同受众频道，建议建立独立组织包，避免品牌混同。

## 三、为新公众号生成对应资产

先生成资产计划：

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/orgs.py" asset-plan \
  organizations/new-account-id recruitment \
  --route confirmed-route \
  --background-mode generated-family \
  --output output/new-account-id/recruitment-asset-plan.json
```

`--route` 和 `--background-mode` 不设默认值，必须与启动时已确认的风格与底图选择一致；不生成底图时改为 `--background-mode native-surfaces`。

资产计划会根据该组织的：

- 品牌色与视觉母题；
- 文章类型；
- 当前视觉路线；
- 已有素材库；
- 真实照片与生成图像的边界；

输出每个通用资产位的状态、建议尺寸、保存目录和生图提示蓝图。它不预造四枚泛化微组件；分镜完成后，`build_visual_kit.py` 才按启动时确认的 `micro_component_count` 与 `selected_roles` 为当前推文生成 0–4 枚真实相关小组件。

系统可以生成：

- 封面或开场背景；
- 章节主视觉；
- 透明背景点缀插画；
- 流程、系统与技术解释图；
- 照片裁切、压缩和派生版本；
- 与组织视觉路线一致的组件背景资产。

不会生成：

- Logo 或组织标志；
- 报名码、群二维码或公众号二维码；
- 冒充真实人物、活动现场或项目成果的图片；
- 带有生成中文、虚假数据或合作伙伴标志的图像。

将用户提供或已审核的文件放入对应目录。对于不透明的 `generated-illustrative` 底图或纯生成 raster 封面，先保留母版并生成带来源水印的 final derivative；公开报告留在 pack 内，raw watermark ID 的私密记录必须留在 pack 和 Git 之外。先创建私密目录并设定 `PROVENANCE_WATERMARK_PRIVATE_ROOT`；`--private-record` 必须位于该目录下：

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/provenance_watermark.py" embed \
  organizations/new-account-id/assets/generated/recruitment-hero-raw.png \
  organizations/new-account-id/assets/derived/recruitment-hero-final.png \
  --key-epoch 1 \
  --report organizations/new-account-id/assets/derived/recruitment-hero-watermark.json \
  --private-record /secure/private-registry/recruitment-hero.json
```

1024×1536 等常见底图若超过 V1 嵌入像素上限，加 `--resized-carrier organizations/new-account-id/assets/derived/recruitment-hero-carrier.png`。工具会一次性创建等比 LANCZOS carrier，在报告绑定原图/carrier 的 SHA 和尺寸；不会独立抬高短边，极端长宽比直接失败。登记时 `--watermark-source` 指向 carrier，再用 `--watermark-original-source` 指向 1024×1536 原图；验收会在内存中重算 carrier 字节，不只信报告里的尺寸。

只有报告达到 `local_verified`、`PSNR >= 42 dB` 且完整画面通过 `390px-if-larger → JPEG Q75` 模拟才注册 final 文件：

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/orgs.py" register-asset organizations/new-account-id \
  --id visual.recruitment-hero \
  --kind background \
  --title "Recruitment hero background" \
  --location assets/derived/recruitment-hero-final.png \
  --watermark-source assets/generated/recruitment-hero-raw.png \
  --watermark-report assets/derived/recruitment-hero-watermark.json \
  --origin generated-illustrative \
  --style campus-editorial \
  --use recruitment \
  --visual-role illustrative-atmosphere \
  --background-family-id current-family-v1 \
  --background-variant master
```

登记时会用外部密钥重新检测当前像素、独立重跑传输模拟、回算母版/final/公开报告 SHA 与 PSNR，并拒绝源文件覆盖、伪造报告、未知字段、低于视觉门槛的结果或 pack 外路径。成品、公开报告和私密记录都不覆盖已存在路径。真实照片、用户/官方图片、Logo、二维码、透明 `article-micro`、SVG/SMIL 与 QA 截图不修改。V1 不承诺裁切、加边、旋转或透视变换后的截图检出；完整合同见 [provenance-watermark.md](provenance-watermark.md)。

日常查水印用 `detect`（密钥仍由外部 secret manager 注入）：

```bash
audit_dir="$(mktemp -d)"
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/provenance_watermark.py" detect \
  "/absolute/path/recruitment-hero-final.png" \
  --report "$audit_dir/local-detect.json"
```

退出码 `0` 且报告同时出现 `status: payload_authenticated` / `authenticated: true` 才是通过。本地和微信托管图都要核对 `payload_fingerprint`、`key_epoch`、`version`、`purpose`、`algorithm`；本地图还要求 `input_sha256` 对齐 embed 报告 `post_sha256`。微信端需先下载保存草稿实际返回的 `mmbiz.qpic.cn` 完整图，再运行同一命令；不用截图、裁切图或转码图。退出码 `1` 仅表示 `not_detected`，`2` 表示密钥/输入/路径/格式错误。详见 [provenance-watermark.md](provenance-watermark.md#detection-commands)。

companion 使用相同 family ID，并改为 `--background-variant companion`；在 `organization.visual.calibration.background_family` 同时登记 master、1–3 个 companion，以及：

```json
{
  "surface_mode": "dark",
  "copy_safe_zone": {"x": 0.12, "y": 0.16, "width": 0.76, "height": 0.58},
  "body_text_color": "#FFFFFF",
  "minimum_contrast_ratio": 4.5,
  "maximum_copy_safe_stddev": 0.10
}
```

登记完全部底图后必须再次运行 `python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" "$ORG_WECHAT_RUNTIME_ROOT/scripts/orgs.py" validate organizations/new-account-id`。不允许用一张浅色、一张深色拼成 family，也不能在验证失败时进入全文排版。

Logo 或二维码的注册示例：

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/orgs.py" register-asset organizations/new-account-id \
  --id brand.logo \
  --kind logo \
  --title "Canonical logo" \
  --location assets/official/logo.png \
  --origin official \
  --style canonical \
  --use all
```

## 四、在 Ardot 生成可编辑文章

在读取本篇材料前，一次性询问并确认四项：小组件数量 `0–4`、是否使用 SVG、风格路线、是否生成背景底图。不要静默套默认值。根据 `references/article-schema.md` 创建 `article.json`，把答案写入 `production_preferences`：

```json
{
  "route": "field-notes",
  "production_preferences": {
    "status": "confirmed",
    "confirmed_by": "user",
    "micro_component_count": 2,
    "use_svg": true,
    "style_route": "field-notes",
    "generate_backgrounds": false
  }
}
```

`style_route` 必须等于文章 `route`。新组织尚无 route 名称时，先记录用户选择的 source-zero / reviewed-preset / 明确参考方向，校准后将它绑定到最终批准 route；不得偷偷换风格。然后写 4–10 个叙事章节，确定每章命题、构图、视觉意图与 block 覆盖：

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/build_storyboard.py" article.json \
  --output output/new-account-id/article-slug/storyboard-plan.json
```

分镜批准后先写 `article.interaction_plan`。`use_svg: true` 使用 `authoring_mode: dynamic-default` 与 2–3 个 semantic modules：2 个必须分布在实际 early + middle chapters，3 个再增加 late。`use_svg: false` 使用 `authoring_mode: static-selected`、`target_module_count: 0`、`modules: []`，无需再写重复的静态例外。每个动态 module 绑定该章 `source_block_indices` 和一个具体读者任务；四个部门分别点击仍只算 1 个 module，但四个 transport instances 要分别登记原文、唯一 fallback key 与 `sha256:<64 hex>`。这套门槛与微型视觉套件相互独立，不能用微插图、艺术字或装饰动画凑数。字段见 `references/interaction-composition.md`。

只有 `ready_for_visual_kit: true` 才生成文章专属小组件/小插图计划：

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/build_visual_kit.py" article.json \
  --org organizations/new-account-id \
  --output output/new-account-id/article-slug/visual-kit-plan.json
```

在 `visual_kit.selected_roles` 写入与确认数量完全一致的角色子集，再只为这些角色生成文章专属微型视觉资产；数量为 0 时使用 `status: not-requested`、空角色和空资产。每枚图必须写明 `storyboard_chapter`、正文原句 `source_text`、`concrete_subject`、`action`、`composition_role` 和 `placement`。不再把整篇正文粗暴塞进通用生图提示词。

Codex Desktop 必须先加载仓库内 `chatgpt-web-image-route` 和已安装的 `codex-with-chatgpt`。它只使用内置 Browser 操作 ChatGPT，不使用 Computer Use 或外部 Chrome。为每个真实组件选择适合的 provider-original PNG 源：可直接要求真透明，也可明确要求计划给出的纯色 key 背景；通过页面真实下载动作保存到 `assets/generated/`。截图、预览 Canvas、剪贴板或复制 URL 都不算原图。C2C doctor 成功也只证明连接，不证明生图或 RGBA。

每张正式原图先生成 `org-wechat-provider-image-acquisition-v2`：绑定已验证 installed release 的 census、当前 adapter 字节及其 `generation_route_id`、同 provider session runtime binding、canonical request metadata，以及 Browser 观察下载路径经 `ingest_browser_download.py` 产生的 create-once report。ingestion target 必须就是后续 processor 读取的 raw PNG，SHA-256 和 byte length 都会重算。完整合同见[provider acquisition authority](provider-acquisition-authority.md)。

Codex Desktop 的当前会话路线可以在 runtime session binding、canonical request、create-once ingestion、exact raw bytes 和真实素材自身质量链通过后正式 operational accept。合成 RGBA migration probe 不再是前置条件。这是当前会话的 operator/harness trust，不是宿主签名证明。

```python
from provider_acquisition_authority import live_provider_acquisition_authority
from prepare_micro_cutout import prepare_micro_cutout
from orgs import command_register_asset
from build_visual_kit import build_visual_kit_plan

# 返回 True 只表示不 veto，不产生 host_attested/portable 宣称。
policy_hook = host_adapter.provider_acquisition_policy_hook()
with live_provider_acquisition_authority(policy_hook):
    prepare_micro_cutout(...)
    command_register_asset(register_args)
    plan = build_visual_kit_plan(article_path, organization_pack)
```

只有 migration 与 provider 两份 Ed25519 receipt 都通过时，standalone CLI 才能宣称 portable 强保证。下例中的信任库位于仓库外、受保护且当前进程不可写；命令只验签，不签发：

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/prepare_micro_cutout.py" \
  organizations/new-account-id/assets/generated/article-object-raw.png \
  organizations/new-account-id/assets/derived/article-object-cutout.png \
  --role floating-spot \
  --article-id ARTICLE_ID \
  --asset-slot-id kit.floating-spot \
  --prompt-sha256 sha256:PROMPT_SHA \
  --generation-route chatgpt-web-image-route-v1 \
  --acquisition-report organizations/new-account-id/assets/generated/article-object-acquisition.json \
  --portable-trust-store /PROTECTED/HOST-PUBLIC-KEYS.json \
  --require-native-alpha \
  --report organizations/new-account-id/assets/derived/article-object-cutout.json
```

真透明源使用 `--require-native-alpha`；明确生成的均匀、边界连通纯色源可在第一轮直接使用计划中的 `--key-color '#KEYHEX'`。处理器不猜背景，且原始素材的灵活性绝不放宽派生结果：最终仍必须是 RGBA8 真透明、紧裁切、无烘焙棋盘格、low-Alpha 色雾、halo、碎片或底板。背景不均、主体碰边、透明材质无法安全分离时停止该槽位，不得继续换 key、降低最终阈值或对任意照片强制抠图。然后逐张运行真实 Alpha、尺寸与角色宽高比检查：

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/inspect_asset.py" \
  organizations/new-account-id/assets/derived/article-object-cutout.png \
  --role floating-spot
```

先把登记 ID 写入 `article.visual_kit.assets`；此时若尚未有 Ardot component node，计划仍会保持 `ready_for_layout: false`。当前会话的注册由上面的 host adapter API 在同一 callback context 中调用；下面 standalone 资产注册示例仅适用于 portable-signed：

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/orgs.py" register-asset organizations/new-account-id \
  --id spot.article-object \
  --kind illustration \
  --title "Article object spot" \
  --location assets/derived/article-object-cutout.png \
  --origin derived \
  --cutout-report assets/derived/article-object-cutout.json \
  --portable-trust-store /PROTECTED/HOST-PUBLIC-KEYS.json \
  --style article-specific \
  --use recruitment \
  --role floating-spot \
  --generated-for ARTICLE_ID \
  --visual-role article-micro
```

所有选中角色不能共用图片；每类的 `asset_slot_id` 必须精确是 `kit.<role>`。每枚派生图都必须在资产登记中明确绑定当前 `article_id`、census/adapter/runtime session、ingestion/raw、原图/prompt/路由/处理器/配置/报告 SHA 与 bytes。正式透明图仍逐张验收，但这不是启动或读材料门禁。

然后为本篇文章选定 2–4 个 Hero/章节/观点/关键短语/CTA 表现型字体时刻。每个时刻必须引用正文原句和批准的 `recipe_id`，至少实现两种非字体构造手法，并在 `construction` 中写回独立的 Ardot 文本/矢量 node IDs；主文本的 `file_url/node_id/style_id/name` 也必须完整。只换字体会直接失败。完整字段见 `references/expressive-typography.md`。

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/build_ardot_manifest.py" article.json \
  --org organizations/new-account-id \
  --output output/new-account-id/article-slug/ardot-manifest.json
```

清单会包含：

- 目标 Ardot 文件、页面、变量集和组织模式；
- 本篇采用的视觉路线与组件变体；
- 每个区块的真实 Ardot 组件名和覆盖内容；
- 需要上传的组织资产及本地路径；
- 视觉 QA 阻断项与微信投递边界。
- 每个区块的开放式构图模式、微型视觉角色与方框比例限制。

如果 `ardot.json` 仍是 `not-linked`，先在 Ardot 中建立或选择组件系统，再填写：

- `design_file.url` 与 `file_id`；
- `variable_mode`；
- `page_names`；
- `component_aliases`。

随后按清单在 Ardot 中：

1. 按 `micro_component_count` 把 `selected_roles` 中已选的 0–4 类小插图做成无默认底色/描边/圆角的原生 Ornament 组件；数量为 0 时跳过，并确认表现型字体仍是可编辑文本节点。
2. 应用该组织的变量模式。
3. 获取或创建 Hero、章节、观点、步骤/流程、案例和 CTA 组件变体。
4. 建立 390 px 文章根 Frame；所有区块默认开放、无外框。
5. 仅在 `use_svg: true` 时为每个 interaction module 建一个原生 group component，保留 `closed`、`open`、`fallback` 三态，并把三态 node、390 px 截图、文件哈希、当前 revision 和组内 instance/hash 覆盖列表写回 `interaction_plan`；`use_svg: false` 时保持零模块并跳过。
6. 用插图切入文字边缘、章节转场、连续路径和 CTA 落点，不能用方框替代视觉节奏。单个微图片层不超过 72% 行宽、整个实例不超过 82%；含字实例不加文字框/底板，主短句至少 22 px 且为局部正文的 1.35 倍。
7. 按叙事章节装配，不按“一个 block = 一个带外框组件”机械堆叠，也不把动态模块相邻堆成组件墙。
8. 从同一 article root 分别导出 Hero、章节、证据、复杂区块和 CTA 五个不同 Ardot 节点的本地 390 px PNG，记录 node、chapter、SHA-256、实际宽高与 capture 时间到 v3 `visual-review.json`。
9. 从同一根导出 `ardot-article-instance-inventory`，只列出 `selected_roles` 的全部实际 instance；数量为 0 时应为空。再为每个实际 instance 导出 `ardot-node-properties` JSON，包含 instance/image/text/closed-shape bounds、font size、fill/stroke 和 node IDs。逐文件记录 SHA-256，并把 placement 绑定到对应截图 SHA。不要手填宽度比例、偏移或“无框”。
10. 固定检查主体相关性、风格一致、照片/插图和谐、泛 AI 装饰、无意义标签、编辑节奏、手机可读性、开放构图、信息密度、表现型字体和无烘焙字图。仅当组件数大于 0 时再检查装饰裁切、尺度变化、无文字框、无全宽微图、错落构图和字号层级；仅当 `generate_backgrounds: true` 时检查底图家族一致性，否则检查 Ardot 原生 surface 的全文连续性与正文对比度。
11. 为五个截图节点记录与截图哈希绑定的密度样本。默认 `compact-editorial`：正文 15–17 px、行高 1.45–1.62、中文字距 -0.2–0 px、段距 8–14 px、章内主间隔 24–40 px、普通正文区内容占用率 68%–90%、最大无意空洞不超过 20%。
12. 视觉确认后将 Ardot 保留为唯一可编辑源。

详细规则见 `references/ardot-workflow.md`。

## 五、生成微信投递文件

先验证独立截图验收文件：

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/build_visual_review.py" visual-review.json --article article.json
```

把路径写入 `article.visual_review_file`。只有验收通过后，才从当前 Ardot root 冻结 handoff v5 与 `ardot-current-root-layer-export-v1`。编译前再通过当前 Ardot 宿主读取同一精确 file/root，保存到另一份 `qa/live-current-root.json`；不得复制冻结文件充当 live reread。

无 signer 时使用 `current-session-draft`：

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/wechat_publisher.py" \
  --store delivery/publisher.sqlite3 prepare-uploads handoff.json \
  --target-account appid:EXACT_APPID \
  --output delivery/upload-map.json
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/validate_transport_fidelity.py" handoff.json \
  --intended-html output/new-account-id/article-slug/wechat-candidate.html \
  --live-root-export qa/live-current-root.json \
  --upload-map delivery/upload-map.json --require-upload-map \
  --require-live-root --session-draft
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/compile_wechat.py" \
  --transport-fidelity handoff.json \
  --live-root-export qa/live-current-root.json \
  --upload-map delivery/upload-map.json \
  --session-draft \
  --output output/new-account-id/article-slug \
  --check
```

`delivery/` 必须事先存在。`prepare-uploads --output` 是 create-once：已有输出、用户 symlink 路径或无法预留 journal 都会在上传前停止。它会在同目录生成 `.<输出文件名>.upload-journal.jsonl`，追加哈希链事件，并绑定 canonical `publisher.sqlite3` 路径与持久 store identity。已知失败可用原命令、原 store、原输出续跑；续跑前会逐笔核对 journal committed 事件与 SQLite `complete` 行。换库、丢行、`pending` 或 `ambiguous` 一律先人工对账，不得删改 journal/map 或换名规避。

该命令产生 `wechat-candidate.html` 和 `candidate-report.json`。只有当前宿主工具轨迹同时显示真实 Ardot reread 和后续微信操作时，宿主才能把精确绑定的 candidate 作为本次可逆草稿写入的动作输入。未签名报告只证明结构对应，必须为 `candidate_valid: true`、`draft_write_eligible: false`、`portable_audit_verified: false`、`delivery_eligible: false`，不得把宿主动作权序列化为本地 artifact 授权。

有 `host.receipt.attest` 并需要可携带审计时，使用 `portable-signed-audit`；宿主从真实工具响应签发短时效 `qa/live-current-root-receipt.json`，仓库只验签不签发：

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/validate_transport_fidelity.py" handoff.json \
  --intended-html output/new-account-id/article-slug/wechat.html \
  --live-root-export qa/live-current-root.json \
  --live-root-receipt qa/live-current-root-receipt.json \
  --upload-map delivery/upload-map.json --require-upload-map \
  --require-live-root
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/compile_wechat.py" \
  --transport-fidelity handoff.json \
  --live-root-export qa/live-current-root.json \
  --live-root-receipt qa/live-current-root-receipt.json \
  --upload-map delivery/upload-map.json \
  --output output/new-account-id/article-slug \
  --check
```

`article.json` 模板适配器只能显式作为调试预览，它不产生可投递 `wechat.html`：

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/compile_wechat.py" article.json \
  --org organizations/new-account-id \
  --authoring-preview \
  --output output/new-account-id/article-slug/authoring
```

输出目录包含：

- `visual-kit-plan.json`：文章专属小组件/小插图生产与验收清单。
- `storyboard-plan.json`：叙事章节、构图和 block 覆盖校验。
- `ardot-manifest.json`：Ardot 开放式构图装配与 QA 清单。
- `visual-review.json`：实际 Ardot 节点的 v3 截图、哈希、尺寸、capture、密度、微组件 inventory 与 node-property 检查证据。
- `index.html`：冻结图层投递预览，不是设计源。
- `wechat-candidate.html` / `candidate-report.json`：`current-session-draft` 的精确结构绑定；可作为本次宿主中的草稿写入输入，但不是 portable/final 证据。
- `wechat.html` / `compile-report.json`：只在 `portable-signed-audit` 中由冻结 Ardot 图层和有效 live receipt 生成的终态内联传输链；chapter y 连续覆盖当前 root，每个可见图层绑定真实 source node、受支持字体/渲染 style 与独立 asset SHA。
- `authoring-preview.html`：从 article JSON 模板生成的非投递调试件，报告 `delivery_eligible: false`，严禁导入公众号。
- 候选/终态报告不可互换：`candidate-report.json` 只表示 current-session 结构通过；`compile-report.json` 记录 host-signed live-root preflight、transport source/revision、postflight和最终 `wechat.html` path/bytes/receipt 绑定。
- `interaction-policy-report.json`（存在动态候选时）：固定 policy、静态回退完整性、保存回读、移动端能力档案与最终 payload 选择。
- `assets/`：本篇文章实际使用的本地图片副本。

投递前将当前 Ardot root 的完整可见文本节点顺序导出为 `ardot-current-root-export`，将其与完整图层 export 写入 handoff v5，再按所选保证档运行对应 gate。下例为 `portable-signed-audit`：

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/validate_workflow_attribution.py" handoff.json
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/validate_transport_fidelity.py" handoff.json \
  --html output/new-account-id/article-slug/wechat.html \
  --live-root-export qa/live-current-root.json \
  --live-root-receipt qa/live-current-root-receipt.json \
  --upload-map delivery/upload-map.json --require-upload-map \
  --compile-report output/new-account-id/article-slug/compile-report.json \
  --require-compile-report
```

保存并重新打开草稿后，从实际微信正文提取可见文本（不是源 HTML），并导出逐章 readback。`current-session-draft` 运行：

这里的 `saved-draft-readback.json` 不能手写、复制旧报告或只拿 Ardot 截图拼装。无 signer 的 API 草稿必须先按同一安装 release 中 `skills/ardot-wechat-publisher/SKILL.md` 的 **Read back and verify** 完整执行：publisher `capture-raw` 保留刚发生的 `draft/get` 原始响应；同一 Browser/Computer Use session 打开精确账号/草稿并为每章截取真实 `390 × chapter_height` PNG；`ingest_wechat_readback_capture.py` 将这些 create-once 字节绑定到 census/profile/report/session/account/draft/revision/raw request 和一次性 nonce；最后由 publisher `capture-readback --capture-bundle` 生成下方验证所用 readback。三类输出都必须在安装 runtime/Skills 之外、已存在且无 symlink 的父目录中，目标本身必须尚不存在。Bundle 固定 `host_attested=false`、`portable=false`、`publication_authority=false`；缺 `host.receipt.attest` 不阻断这条草稿回读链，但它绝不授权发表。Portable signed 路线继续使用独立 `--screenshots` manifest 与 signed receipt，不能混用 capture bundle。

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/validate_workflow_attribution.py" handoff.json \
  --saved-draft-visible-text saved-draft-visible-text.txt \
  --require-readback
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/validate_transport_fidelity.py" handoff.json \
  --html output/new-account-id/article-slug/wechat-candidate.html \
  --live-root-export qa/live-current-root.json \
  --upload-map delivery/upload-map.json --require-upload-map \
  --compile-report output/new-account-id/article-slug/candidate-report.json \
  --require-compile-report \
  --expected-target-account 'exact-account-ref-from-delivery-preflight' \
  --readback saved-draft-readback.json --require-readback --session-draft
```

这次写入、重开与导出必须同时可见于当前宿主轨迹，验证报告必须有 `session_readback_structural_match: true` 和 `portable_audit_verified: false`。`portable-signed-audit` 再跑：

```bash
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/validate_workflow_attribution.py" handoff.json \
  --saved-draft-visible-text saved-draft-visible-text.txt \
  --require-readback
python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
  "$ORG_WECHAT_RUNTIME_ROOT/scripts/validate_transport_fidelity.py" handoff.json \
  --html output/new-account-id/article-slug/wechat.html \
  --live-root-export qa/live-current-root.json \
  --live-root-receipt qa/live-current-root-receipt.json \
  --upload-map delivery/upload-map.json --require-upload-map \
  --compile-report output/new-account-id/article-slug/compile-report.json \
  --require-compile-report \
  --expected-target-account 'exact-account-ref-from-delivery-preflight' \
  --readback saved-draft-readback.json \
  --readback-receipt saved-draft-readback-receipt.json \
  --require-readback
```

所选模式的全部门禁都必须退出 `0`。内容门禁证明感谢语原生可编辑、可见、非栅格且末位；两档传输门禁都核对每章 section/text/asset、hosted 下载字节、390 px 草稿截图和账号/草稿 ID。只有 portable 档使用宿主 Ed25519 receipt 鉴真完整字节链；current-session 档必须连同当前宿主轨迹一起审查，不得脱离会话声称可携带审计。默认终止于草稿；正式发表/群发需另行当次明确确认，且 publisher 必须先通过权威草稿回读和状态门禁。current-session API 发布还必须由当前宿主进程注入 live-authority callback，现场重读精确 Ardot/账号/草稿并消费用户确认事件；单独运行 CLI 或写一份“host trace/确认 JSON”必须 fail closed。该 live 路线仍固定 `portable_audit_verified: false`。

`--check` 会阻断缺失/未确认的启动偏好、风格 route 不一致、无效视觉 provenance（source-zero 隔离未确认，或 explicit grammar 缺来源/scope/non-copy/SHA）、未校准/仍为 provisional、选择生成底图却没有合格 family、缺少分镜、选定微组件的数量/角色/独立性不匹配、缺原图下载与派生 lineage、最终 RGB/全不透明伪 RGBA、无真 Alpha、low-Alpha 残留、彩色 halo/碎片/纹理底板、呈矩形透明边卡片、无 Ardot node 证据、缺失或未覆盖全部实际微组件实例、全宽微图、文字框、按数量缩放后的错落或字号层级不足、缺少或烘焙的表现型字体、SVG 开关与 dynamic-default/static-selected 计划冲突、instance key/hash 不唯一、当前 revision 三态证据不完整、不完整或哈希不符的五类 Ardot 截图，以及任一视觉检查失败的文章。

## 六、投递到公众号

1. 通过目标公众号连接上传正文图片，将本地路径替换为该账号返回的微信图片 URL。SVG `<image>` 只能使用 `mmbiz.qpic.cn` URL。
2. 单独把当前封面上传到目标账号永久素材，取得该账号专属 `media_id`，并在草稿 payload 中设为 `thumb_media_id`。正文图片 URL、SVG 图片和另一账号的素材 ID 均不能代替封面。
3. 若存在动态候选，先运行固定策略校验：

   ```bash
   python3 -I -S "$ORG_WECHAT_RUNTIME_ROOT/scripts/secure_runner.py" \
     "$ORG_WECHAT_RUNTIME_ROOT/scripts/wechat_interaction_policy.py" candidate.html \
     --fallback fallback.html \
     --output interaction-policy-report.json
   ```

   只允许无 JavaScript、无 ID、元素自身 `begin="click"` 的 `<set>` / `<animateTransform>` 与 CSS 横滑；`details`、跨 ID timing 和无静态等价物直接阻断。
4. 创建或更新一个草稿，然后回读标题、摘要、正文顺序、图片 URL、实际封面、fallback 哈希和 SMIL 结构签名。结构回读成功只代表未被清洗，不代表手机端会执行动画。
5. 动态版只有在目标账号相同、策略版本相同且未过期的 iOS/Android 真机能力档案通过后才可保留；否则更新同一草稿为静态回退并再次回读，不新建重复草稿。
6. 无 API 授权时可退回浏览器导入，但必须保存后重新打开并实际看到封面与正文，不能把“粘贴完成”报告成“已验证草稿”。
7. 正式发布前再次征得明确确认。

组织包中不保存 AppSecret、access token 或其他账号密钥。

## 七、迁移后能否自动生成对应文件资产

可以，但前提是先完成一次组织建模与视觉校准。之后每篇文章都能稳定生成：

- 该公众号对应的 Ardot 原生长文画板；
- 复用或新增的组织组件实例；
- 每篇文章先生图得到的专属微型视觉套件；
- 与路线一致的封面/章节/解释图资产计划；
- `ardot-manifest.json` 装配清单；
- 视觉通过后的微信草稿投递文件。

系统不会假设“所有 AI 组织都长一样”。迁移结果由组织变量模式、路线、组件变体、真实素材和文章类型共同决定。
